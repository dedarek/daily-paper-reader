---
title: "Neuro-Agentic Control: A Deep Learning-based LLM-Powered Agentic AI Framework for Controlling Security Controls"
title_zh: 神经代理控制：一种基于深度学习的LLM驱动的代理型AI框架用于安全控制
authors: "Saroj Gopali, Bipin Chhetri, Deepika Giri, Sima Siami-Namini, Akbar Siami Namin"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09076v1"
tags: ["query:llm-security"]
score: 8.0
evidence: 神经代理控制框架，通过反事实物理注入实现安全代理控制
tldr: "工业物联网中传统规则监控难以应对复杂攻击，LLM虽能语义推理但幻觉导致安全隐患。提出神经-代理控制框架，将LLM规划器与预训练时序基础模型TimesFM结合，通过反事实物理注入在动作前模拟干预的物理影响，拒绝不安全动作。在SWaT数据集上，框架防止了33.3%的违规，优于LSTM（26.7%）和TCN（13.3%），且零幻觉动作执行。该方法证明了基础模型作为确定性哨兵保障代理AI在关键基础设施中安全性的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09076v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1858, \"height\": 471, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09076v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 846, \"height\": 176, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09076v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1637, \"height\": 1114, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09076v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1944, \"height\": 609, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09076v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 930, \"height\": 216, \"label\": \"Table\"}]"
motivation: 传统规则监控在工业物联网中无法有效应对攻击，而LLM的幻觉问题使其无法用于闭环控制。
method: 耦合LLM规划器与TimesFM，通过反事实物理注入在动作前模拟物理影响并拒绝不良干预。
result: "在SWaT数据集上，框架防止了33.3%的违规，优于LSTM的26.7%和TCN的13.3%，且无幻觉动作执行。"
conclusion: 将基础模型作为确定性哨兵，可有效保障代理AI在关键基础设施中的安全运行。
---

## 摘要
针对操作技术的网络攻击日益导致高昂的停机时间和物理损坏，暴露了工业物联网环境中传统基于规则监控的局限性。虽然大语言模型（LLM）具有强大的语义推理能力可辅助决策支持，但其幻觉特性对闭环控制构成了不可接受的安全风险。本文提出了一种神经代理控制框架，这是一种新颖的架构，将基于LLM的规划器（例如Gemini 2.5 Flash-Lite）与预训练的时间序列基础模型（TimesFM）相结合，以实现基于物理的自主防御。该框架引入了一种“反事实物理注入”机制，在执行前在基础模型的数值隐空间中模拟LLM提议干预的影响，同时允许系统拒绝幻觉或不安全的动作。在随机攻击场景下，基于工业数据集（例如安全水处理SWaT）的评估显示，该框架相比LSTM和TCN基线表现出更优性能。神经代理循环阻止了五次低于阈值的违规（33.3%），而LSTM为26.7%，TCN为13.3%，且未执行任何物理无效（幻觉）动作。这些结果证明了使用基础模型作为确定性“哨兵”来保护关键基础设施中代理型AI的有效性。

## Abstract
Cyberattacks on operational technology are increasingly causing costly downtime and physical damage, exposing the limitations of traditional rule-based monitoring in industrial IoT environments. While Large Language Models (LLMs) have strong semantic reasoning abilities to assist in decision support, their hallucinatory nature presents unacceptable safety liabilities for closed-loop control. This paper introduces a neuro-agentic control framework, a novel architecture that couples an LLM-based planner (i.e., such as Gemini 2.5 Flash-Lite) with a pre-trained Time-Series Foundation Model (TimesFM), to achieve physics-grounded autonomous defense. The paper introduces a ``Counterfactual Physics Injection'' mechanism that simulates the impact of LLM-proposed interventions within the numerical latent space of the foundation model before actuation, while allowing the system to reject hallucinatory or unsafe actions. Evaluated on an industrial dataset (e.g., the Secure Water Treatment (SWaT)) in the context of stochastic attack scenarios, the framework exhibited better performance compared to LSTM and TCN baselines. The Neuro-Agentic Loop prevented five breaches (33.3%) below the threshold versus LSTM (26.7%) and TCN (13.3%), with zero physically invalid (hallucinated) actions executed. These results demonstrate the efficacy of using foundation models as deterministic ``Sentinels'' to safeguard agentic AI in critical infrastructure.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：工业物联网（IIoT）中，针对操作技术的网络攻击导致生产停线、物理损坏和高昂经济损失（如汽车行业每小时损失超200万美元）。传统基于规则的手动监控难以应对大规模、高动态的工业环境，且现有的LLM虽能进行语义推理辅助决策，但其“幻觉”特性（生成不安全或物理无效的动作）使其无法直接用于关键基础设施的闭环控制。
- **整体含义**：本文旨在解决LLM在安全攸关工业控制系统中的可靠性问题，提出一种将LLM的语义推理与预训练时间序列基础模型的物理预测相结合的神经代理控制框架，以实现可自主防御的安全控制，同时确保不执行任何幻觉动作。

## 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：将系统分为两个角色——**规划器（Architect，基于LLM如Gemini 2.5 Flash-Lite）** 负责生成候选干预策略（语义级），**哨兵（Sentinel，基于预训练TimesFM）** 负责对策略进行物理仿真验证（数值级），通过“反事实物理注入”机制模拟动作效果，仅选择降低风险的最优策略执行，拒绝不安全或无效策略。
- **关键技术细节**：
  - **反事实物理注入（Counterfactual Physics Injection）**：在不修改TimesFM内部状态的前提下，将LLM提议的控制动作（包含排水速率μ和持续时间δ）转化为对预测值的线性扰动。公式如下：  
    \[
    \hat{y}'_t = \begin{cases}
    \hat{y}_t + \mu \cdot (t+1) & \text{if } t < \delta \\
    \hat{y}_t + \mu \cdot \delta & \text{if } t \geq \delta
    \end{cases}
    \]  
    该扰动模拟了动作开始后水位的累积下降，以及动作结束后的保持状态。
  - **幻觉拒绝机制**：两类拒绝——①物理无效幻觉动作（如正幅值充水、超出执行器边界）；②虽有效但被其他候选策略支配。最终选优公式：  
    \[
    a^* = \arg\min_{c \in C \cup \{\emptyset\}} \left( \max_{t \in H} \mathcal{F}(\Phi(\hat{Y}, c)) \right)
    \]  
    若所有候选策略的模拟风险不低于无行动基线，系统默认“监控”模式，拒绝所有LLM提议。
- **算法流程（文字说明）**：
  1. **异常检测**：Sentinel持续预测未来窗口，若预测峰值超过安全阈值θ，触发主动控制。
  2. **策略生成**：通过RAG增强提示，Architect生成k个候选动作（JSON格式，含名称、幅值μ、持续时间δ），且必须满足物理约束（μ<0, |μ|≤60, 5≤δ≤50）。
  3. **可行性验证**：检查每个候选动作的物理合法性。
  4. **反事实物理注入**：对每个合法候选，使用上述公式修改原始预测，得到修正后的峰值R_sim。
  5. **确定性风险评估与选择**：选择使R_sim最小的候选动作作为最终输出a*；若无改进则返回“监控”。

## 3. 实验设计

- **数据集与场景**：
  - 使用**SWaT（Secure Water Treatment）** 数据集（2019年7月版本），包含78列、14996个样本，涵盖正常操作（3小时）和攻击事件（1小时）。实验中仅使用**攻击事件**部分，目标变量为**LIT301**（非线性的水箱水位）。
  - 随机注入三类攻击（各5次，共15次试验）：
    - **突增（Sudden Spike）**：+150mm/20步
    - **缓慢漂移（Gradual Drift）**：+100mm/100步
    - **高噪声（High Noise）**：+80mm/50步
- **基准模型**：
  - **LSTM**（2层，264隐藏单元，dropout 0.2，学习率1e-3）
  - **TCN**（2层，425隐藏单元，dropout 0.2，核大小3，学习率1e-3）
  - 对比条件：LSTM/TCN与TimesFM（Neuro-Agentic框架的哨兵）共用相同的触发、评估和干预框架（但LSTM/TCN不包含反事实注入与LLM规划）。
- **评价指标**：
  - **风险降低（ΔR）**：被动预测峰值与代理干预后峰值的差值。
  - **洪限避免次数**：干预后峰值低于1000 mm阈值的次数。
  - **幻觉拒绝率（HRR）**：哨兵阻止有害动作执行的百分比。

## 4. 资源与算力

- 论文中**未明确说明**训练使用的GPU型号、数量及训练时长。仅提及：
  - TimesFM为预训练模型（200M参数），实验中使用冻结权重进行纯推理。
  - LSTM和TCN的训练配置：batch size=32，epochs=10，学习率1e-3。
  - 推理延迟：单个TimesFM反事实仿真约266ms，完整决策循环（含LLM生成和评估）平均1.5-2.5秒。
- 因此，算力消耗主要来自LLM（Gemini 2.5 Flash-Lite）的调用和TimesFM的推理，但具体平台未披露。

## 5. 实验数量与充分性

- **实验规模**：共15次随机试验（每次试验包含一个攻击模式，三种攻击类型各5次）。每种方法（LSTM、TCN、Neuro-Agentic）均执行相同的15次试验。
- **消融实验**：未进行单独的消融实验（如去掉反事实注入或LLM规划的变体对比）。仅以LSTM和TCN作为非LLM的基线对比。
- **充分性评估**：
  - **优点**：攻击类型覆盖了常见失效模式，且采用随机批测试增加了结果的鲁棒性；提供了详细的逐次试验数据（风险降低、洪限避免、策略选择）。
  - **不足**：
    - 仅使用单一数据集（SWaT）且仅针对单一传感器（LIT301），泛化性有限。
    - 基线LSTM和TCN均在同一触发框架下运行，但未包含“仅LLM无哨兵”的消融以直接量化哨兵作用。
    - 未与已有的LLM基准控制框架（如L2M-AID）进行比较。
    - 15次试验样本量较小，统计显著性未评估。

## 6. 主要结论与发现

- **核心性能**：Neuro-Agentic Loop在15次试验中成功避免了5次洪限（33.3%），优于LSTM（4次，26.7%）和TCN（2次，13.3%）。
- **风险降低幅度**：Neuro-Agentic平均风险降低48.51单位，远超LSTM（16.77）和TCN（20.29），最高单次降低达215.38单位（试验10），是LSTM最大值的3倍以上。
- **零幻觉动作执行**：所有15次试验中，哨兵成功拒绝了所有物理无效或恶化的LLM提议，未执行任何幻觉动作。
- **哨兵效应**：LLM曾提出正向充水的错误策略，但被TimesFM模拟出水位上升曲线后拒绝，证明了时间序列基础模型可作为确定性物理验证层。

## 7. 优点

- **新颖架构**：明确分离语义推理（LLM）与物理控制（基础模型），通过反事实注入实现可验证的闭环安全。
- **高安全性**：零幻觉动作执行，显著降低关键基础设施风险。
- **可解释性**：LLM输出结构化为可读的JSON（幅值、时长），且通过RAG注入运维知识，决策过程可审计。
- **通用性潜力**：反事实注入为闭式线性扰动，不依赖模型微调，可适配任何预训练时间序列模型。
- **对比结果清晰**：相对LSTM/TCN在洪限避免和风险降低上均有显著提升。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅使用SWaT数据集中的单一变量（LIT301），未验证多传感器/多执行器场景。
  - 未包含高保真数字孪生或真实物理实验，线性注入近似忽略非线性动力学（如水力瞬变、耦合）。
  - 15次试验数量较少，缺乏统计显著性检验。
- **方法局限性**：
  - 线性注入模型（Φ(·)）无法替代真实物理模拟，与高保真仿真器存在误差。
  - 推理延迟（1.5-2.5秒）不适用于毫秒级快速机电过程。
  - 扩展至多传感器时，候选策略组合可能导致成本超线性增长。
- **公平性**：
  - LSTM/TCN基线虽共用同一触发框架，但未包含“纯LLM无哨兵”的对比，难以单独量化解码器幻觉的负面影响。
  - 未与现有LLM+强化学习等混合方法（如L2M-AID）进行直接比较。
- **资源未披露**：算力消耗、训练时长、GPU型号等未说明，影响可复现性评估。

（完）
