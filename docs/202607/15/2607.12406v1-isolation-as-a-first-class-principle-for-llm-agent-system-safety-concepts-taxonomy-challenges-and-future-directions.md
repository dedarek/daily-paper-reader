---
title: "Isolation as a First-Class Principle for LLM-Agent System Safety: Concepts, Taxonomy, Challenges and Future Directions"
title_zh: 隔离作为LLM-Agent系统安全的一等原则：概念、分类、挑战与未来方向
authors: "Huihao Jing, Wenbin Hu, Shaojin Chen, Haochen Shi, Sirui Zhang, Hanyu Yang, Changxuan Fan, Zhongwei Xie, Hongyu Luo, Wun Yu Chan, Wei Fan, Haoran Li, Yangqiu Song"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12406v1"
tags: ["query:llm-security"]
score: 9.0
evidence: 关于LLM智能体系统隔离原则的综述，涵盖提示注入、工具滥用、记忆中毒等
tldr: 当前LLM智能体系统面临提示注入、工具滥用、记忆污染等安全威胁，但这些失败的共同结构原因尚未被系统解释。本综述将隔离提升为一阶原则，提出以边界为中心的分类法：用户-智能体、智能体-工具、智能体-执行、智能体-智能体、系统-环境五类边界。通过分析隔离失效的初始边界与跨边界传播路径，系统化归纳了相关防御机制。该工作为构建内建隔离的未来智能体系统提供了研究路线图。
source: arxiv
selection_source: fresh_fetch
motivation: 现有文献分散于攻击类型、应用和基准中，难以解释不同安全失效（如提示注入、工具滥用、记忆污染）的共享结构原因及其在智能体工作流中的传播机制。
method: 提出以隔离为一级原则的边界中心分类法，从用户-智能体、智能体-工具、智能体-执行、智能体-智能体、系统-环境五个边界系统梳理文献，并归纳跨边界失效路径。
result: 识别了五种边界上隔离丢失的首次出现点与跨边界传播模式，总结了每个接口最相关的防御机制，并概述了跨边界失效路径。
conclusion: 隔离应作为构建未来LLM智能体系统安全的先验原则，通过内建隔离设计从源头防止安全威胁的传播与升级。
---

## 摘要
LLM代理作为系统“大脑”的能力从根本上将分析范围扩展到独立模型之外。因此，安全性不再仅涉及输入-输出内容对齐，还涉及系统行为和实际执行结果。然而，当前文献在攻击类型、应用和基准测试方面是零散的。这使得难以解释为何诸如提示注入、工具误用和内存中毒等失败常常共享相同的结构原因，以及它们如何通过代理工作流传播。在本综述中，我们将隔离视为LLM代理系统安全的一等原则。所谓隔离，是指用户输入、工具访问、执行通道、代理间通信以及环境来源上下文的分离。我们使用以边界为中心的分类法组织文献，包括五个边界：用户-代理、代理-工具、代理-执行、代理-代理和系统-环境。这种视角有助于识别隔离丧失首先发生的位置，妥协如何跨边界传播，以及每个接口处最相关的防御措施。我们还总结了跨边界故障路径，讨论了开放挑战，并概述了未来代理系统中“通过构造实现隔离”的研究议程。

## Abstract
The capability of LLM agents to function as the ``brain'' of a system fundamentally expands the scope of analysis beyond a standalone model. Consequently, safety is no longer only about input--output content alignment. It also concerns system behavior and real-world execution outcomes. However, the current literature is fragmented across attack types, applications, and benchmarks. This makes it hard to explain why failures such as prompt injection, tool misuse, and memory poisoning often share the same structural cause, and how they spread through an agent workflow. In this survey, we treat isolation as a first-class principle for LLM-agent system safety. By isolation, we refer to the separation of user inputs, tool access, execution channels, inter-agent communication, and environment-originated context. We organize the literature with a boundary-centric taxonomy of five boundaries: user-agent, agent-tool, agent-execution, agent-agent, and system-environment. This view helps identify where the loss of isolation first occurs, how compromise propagates across boundaries, and which defenses are most relevant at each interface. We also summarize cross-boundary failure paths, discuss open challenges, and outline a research agenda for isolation-by-construction in future agent systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：当前 LLM Agent（智能体）系统的安全性研究分散在不同的攻击类型（如提示注入、工具滥用、记忆中毒）、应用场景和基准测试中，缺乏统一框架来解释这些看似不同的安全失效为何共享相同的结构原因，以及它们如何通过 Agent 的工作流程传播。
- **整体含义**：本文将“隔离”（isolation）提升为 LLM-Agent 系统安全的一等原则，主张通过系统性地分离用户输入、工具访问、执行通道、Agent 间通信以及环境来源上下文，从根源上理解和防御安全威胁。该工作为构建内生安全的未来 Agent 系统提供了理论基石和路线图。

## 2. 论文提出的方法论：核心思想与关键技术细节

- **核心思想**：以“边界”（boundary）为中心的分类法。隔离的丧失首先发生在某个特定边界上，然后可能跨边界传播，最终导致系统级安全失效。
- **五个关键边界**：
  1. **用户-智能体边界**（User-Agent）：用户输入与 Agent 内部状态的隔离。
  2. **智能体-工具边界**（Agent-Tool）：Agent 对工具访问权限的隔离。
  3. **智能体-执行边界**（Agent-Execution）：Agent 决策与实际执行通道的隔离。
  4. **智能体-智能体边界**（Agent-Agent）：多 Agent 系统中通信的隔离。
  5. **系统-环境边界**（System-Environment）：Agent 系统与外部环境（如 Web、数据库）来源上下文的隔离。
- **关键技术细节**：
  - 对每个边界，识别隔离失效的“首次出现点”（first-occurrence point）和“跨边界传播路径”。
  - 在每类边界上，归纳最相关的防御机制（如输入验证、权限最小化、沙箱执行、通信加密、上下文过滤等）。
  - 结合具体攻击案例（如提示注入、工具误用、记忆污染）说明失效路径。
- **算法/流程**：本文是综述性质，没有提出新的算法或公式，但提供了系统化的分析流程：定位失效边界 → 追踪传播路径 → 匹配防御机制。

## 3. 实验设计

- **数据集/场景**：本文为文献综述，没有提出新的实验。它系统梳理了现有文献中涉及的各种攻击场景（如基于 prompt 注入的越狱、工具调用劫持、多 Agent 协作攻击、环境上下文污染等）和防御方案。
- **Benchmark**：未独立构建新的基准测试，而是回顾了现有基准（如 Prompt Injection Benchmark、Tool Misuse Benchmark 等），并指出这些基准的局限性。
- **对比方法**：横向对比了现有防御方法在五个边界上的覆盖程度，但未进行定量实验对比。

## 4. 资源与算力

- 本文未提及任何算力资源（如 GPU 型号、数量、训练时长等）。作为一篇概念性综述，不涉及大规模训练或推理实验。

## 5. 实验数量与充分性

- **实验数量**：无原始实验。作为综述，其“实验”体现在对大量已有研究的系统化分类和归纳。
- **充分性评估**：
  - **充分**：分类法覆盖了 Agent 系统的核心边界，逻辑清晰，能解释众多已知失效案例。
  - **不足**：缺乏对分类法效力的实证验证（例如，在真实 Agent 系统中应用隔离原则后的安全收益量化评估）；边界划分可能忽略混合或新兴边界（如人机协作中的模糊边界）。

## 6. 论文的主要结论与发现

- **结论一**：现有安全威胁（提示注入、工具滥用、记忆中毒等）的共同结构原因是隔离原则的丧失，且失效往往跨边界级联传播。
- **结论二**：隔离应作为 Agent 系统安全的一等（first-class）先验原则，而不是事后修补手段。
- **结论三**：通过“构造即隔离”（isolation-by-construction）的设计范式，可以在系统架构层面预先阻断攻击传播路径。
- **发现**：每个边界上的防御机制存在重叠和互补，未来的安全系统需要整合跨边界防御策略。

## 7. 优点

- **理论创新**：首次将“隔离”提升为 Agent 安全的一等原则，提供了一个统一、简洁的解释性框架。
- **分类法系统化**：五边界分类法覆盖了 Agent 工作流的全生命周期，便于研究者定位具体问题和寻找对应防御。
- **实用性**：不仅分析威胁，还给出了每个边界上最相关的防御机制，对工程实践有直接指导意义。
- **前瞻性**：提出了“通过构造实现隔离”的研究议程，为未来 Agent 系统安全设计指明了方向。

## 8. 不足与局限

- **缺乏实证验证**：作为综述，没有通过实验证明隔离原则在实际系统中的有效性增益，分类法的完整性和实用性尚需更多案例检验。
- **边界模糊问题**：某些攻击可能跨越多个边界同时发生，分类法可能难以区分主因；极端情况下可能存在边界定义不清晰（如智能体-执行边界与智能体-工具边界重叠）。
- **覆盖范围局限**：主要关注 LLM Agent，对于更广泛的具身智能体或混合智能系统可能不适用。
- **动态演化不足**：未深入讨论 Agent 自我进化（如在线学习、记忆动态更新）对隔离边界的影响，以及自适应攻击的应对。
- **防御成本评估缺失**：未讨论实施隔离原则可能带来的性能开销、可用性损失或工程复杂度增加。

（完）
