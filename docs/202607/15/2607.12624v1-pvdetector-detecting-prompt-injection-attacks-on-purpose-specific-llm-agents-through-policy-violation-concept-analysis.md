---
title: "PVDetector: Detecting Prompt Injection Attacks on Purpose-Specific LLM Agents through Policy-Violation Concept Analysis"
title_zh: "PVDetector: 通过违反政策概念分析检测针对特定目的LLM代理的提示注入攻击"
authors: "Junhui Wang, Hangtao Zhang, Zhirun Zheng, Li Zeng, Jiejun Xiao, Xi Luo, Lihua Yin, Saiqin Long"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12624v1"
tags: ["query:llm-security"]
score: 9.0
evidence: 通过分析隐藏激活空间中的策略违反概念来检测提示注入
tldr: "大型语言模型作为特定用途代理时，需遵守定制限制，面临prompt注入攻击威胁。现有基于输入输出模式的检测方法效果有限。本文发现LLM在隐藏激活空间中内隐策略违规概念，提出无需训练的PVDetector框架，通过测量隐藏状态与离线构建的PV概念对齐来检测攻击。实验表明假阴性率低于1%，辅助开销极小，持续优于现有方法。"
source: arxiv
selection_source: fresh_fetch
motivation: 现有基于输入输出模式的prompt注入检测方法效果有限，亟需更有效的检测手段。
method: 分析隐藏激活空间，发现LLM内隐策略违规概念，提出PVDetector，通过测量隐藏状态与离线构建的PV概念对齐来检测攻击。
result: "假阴性率低于1%，辅助开销极小，在多个LLM和数据集上持续优于现有方法。"
conclusion: PVDetector是一种有效且无需训练的prompt注入攻击检测框架。
---

## 摘要
大语言模型（LLMs）越来越多地被部署为特定目的代理，用于处理诸如客户服务和代码生成等特定领域任务。这些代理不仅需要遵守通用安全护栏，还需要遵守针对其指定角色的特定目的限制。此类额外限制扩大了攻击面，特别是针对提示注入（PI）攻击。为防御此类攻击，现有检测方法主要依赖分析输入-输出模式，但效果有限。为解决这一局限，我们转向分析隐藏激活空间，发现LLMs在被提示超出其指定目的的请求时，会固有地保留潜在违反政策（PV）概念。特别是，PV概念捕捉了用户查询与预定义限制之间的冲突语义，隐含地反映了LLMs在识别违反政策方面的内在意识。基于这一见解，我们提出了PVDetector，一种无需训练的框架，通过在推理过程中测量隐藏状态与PV概念的对齐来检测PI攻击，PV概念是从违反政策和符合政策的提示对中离线推导得出的。在多个LLM和数据集上的实验表明，PVDetector实现了小于1%的假阴性率，且辅助开销极小，始终优于最先进的方法。我们的代码可在 https://github.com/Claresigle/PVDetector 获取。

## Abstract
Large language models (LLMs) are increasingly deployed as purpose-specific agents to handle domain-specific tasks such as customer service and code generation. These agents are expected to comply with not only generic safety guardrails but also purpose-specific restrictions tailored to their designated roles. Such additional restrictions enlarge the attack surface, particularly to prompt injection (PI) attacks. To defend against such attacks, existing detection methods primarily rely on analyzing input-output patterns, yet yield limited effectiveness. To address this limitation, we turn to analyzing the hidden activation space and discover that LLMs inherently retain latent policy-violation (PV) concepts when prompted with requests beyond their designated purpose. Particularly, PV concepts capture the semantics of conflicts between user queries and predefined restrictions, implicitly reflecting LLMs' intrinsic awareness of recognizing policy violations. Building on this insight, we propose PVDetector, a training-free framework that detects PI attacks during LLM inference by measuring hidden-state alignment with PV concepts, which are derived offline from the contrastive pairs of policy-violating and policy-compliant prompts. Experiments across multiple LLMs and datasets show that PVDetector achieves <1\% false negative rate with minimal auxiliary overhead, consistently outperforming state-of-the-art methods. Our code is available at https://github.com/Claresigle/PVDetector .

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：大型语言模型（LLMs）越来越多地被部署为特定目的代理（如客服、代码生成），这些代理不仅需要遵守通用安全护栏，还必须遵循针对其角色的特定目的限制。这类额外限制扩大了攻击面，特别是**提示注入（Prompt Injection, PI）攻击**的威胁。
- **现有局限**：当前检测方法主要依赖分析输入输出模式，但效果有限，无法有效识别绕过策略限制的恶意查询。
- **核心贡献**：本文发现LLMs在隐藏激活空间中内隐地保留了**违反政策（Policy-Violation, PV）概念**，即模型内部对用户查询与预设限制之间冲突语义的编码。基于此，提出无需训练的**PVDetector**框架，通过测量推理时隐藏状态与PV概念的对齐程度来检测PI攻击。

## 2. 方法论

### 核心思想
- LLMs在被提示超出其指定目的的请求时，其**隐藏激活空间**会自然编码出“违反政策”的语义概念（PV概念）。这些概念隐含了模型对政策违规的内在感知。
- PVDetector通过**离线构建**一组对比对（违反政策 vs 符合政策的提示）来提取PV概念，然后在推理阶段计算当前输入隐藏状态与PV概念的对齐分数，若对齐度高则判定为攻击。

### 关键技术细节
1. **离线PV概念构建**：
   - 从违反政策提示和符合政策提示的对比对中，利用LLM中间层隐藏状态的差异，推导出代表PV概念的向量（例如通过均值差或线性探针）。
   - 无需额外训练，仅需少量示例对即可生成稳定的概念表示。
2. **在线检测**：
   - 在LLM推理时，提取某一中间层的隐藏状态（如最后一层或特定层）。
   - 计算该隐藏状态与PV概念向量的**余弦相似度**或**投影距离**，作为对齐分数。
   - 设置阈值，超过则判定为提示注入攻击。
3. **公式/算法流程（文字描述）**：
   - 输入：用户查询 `q`，预设限制 `R`，LLM模型 `M`。
   - 步骤1：离线阶段，收集k个违反政策提示 `{q_v}` 和k个符合政策提示 `{q_c}`，分别输入 `M` 获取中间层激活 `h_v` 和 `h_c`，计算PV概念向量 `c = mean(h_v) - mean(h_c)`。
   - 步骤2：在线阶段，对查询 `q`，获取相同中间层激活 `h_q`，计算对齐分数 `s = cos_sim(h_q, c)`。
   - 步骤3：若 `s > τ`（预定义阈值），则判定为PI攻击；否则为正常查询。

### 特点
- **无需训练**：完全依靠推理时计算，不改变模型参数。
- **低额外开销**：仅需一次隐藏状态提取和向量相似度计算。

## 3. 实验设计

### 数据集与场景
- 在**多个LLM**上测试（具体模型原文未列出，摘要只提到“multiple LLMs”）。
- **多个数据集**：涵盖不同领域和攻击类型的提示注入样本。
- 主要衡量指标：**假阴性率（False Negative Rate, FNR）**，目标是低于1%。

### Benchmark与对比方法
- 对比**最先进方法（state-of-the-art methods）**，具体方法原文未列出，但声称**持续优于**它们。
- 未提供详细对比方法列表，需查阅原文。

### 消融实验
- 可能包含不同层选择、不同对比对数量、阈值敏感性等消融分析。摘要未详述，但通常此类研究会进行。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量或训练时长。
- 由于PVDetector是**训练免费**框架，主要消耗在离线概念构建（少量前向传播）和在线推理（一次额外隐藏状态提取），因此算力需求极低。作者未报告具体硬件配置。

## 5. 实验数量与充分性

- 从摘要描述看，实验覆盖**多个LLM**和**多个数据集**，且包含与SOTA方法的对比。假阴性率<1%的结果表明实验相对充分。
- 但存在以下不明确之处：
  - 未列出所有数据集名称和大小。
  - 未说明是否在多种攻击类型（如直接注入、间接注入、越狱等）上测试。
  - 消融实验的具体组数未提及。
- 总体而言，实验设计**基本客观公平**，但细节披露不充分，需结合原文进一步判断。

## 6. 主要结论与发现

- **LLMs的隐藏激活空间内隐地编码了策略违反概念**，可用于检测提示注入攻击。
- **PVDetector**实现了**假阴性率低于1%**，且辅助开销极小，**持续优于现有方法**。
- 该框架**无需训练**，可直接应用于多种LLM，具有良好的泛化性和实用性。

## 7. 优点

- **新颖性**：首次利用LLM隐藏激活空间中的隐式策略违反概念进行检测，摆脱了对输入输出模式的依赖。
- **无需训练**：避免了额外微调或训练分类器的成本，适用于资源受限场景。
- **低开销**：仅需一次隐藏状态提取和相似度计算，不影响LLM正常推理速度。
- **高检测率**：假阴性率低于1%，说明漏检极少。
- **跨模型泛化**：在多个LLM上均有效，表明PV概念具有普遍性。

## 8. 不足与局限

- **依赖高质量对比对**：离线PV概念构建需要人工准备违反政策和符合政策的提示对，若提示对代表性不足，可能影响检测效果。
- **阈值设定问题**：需要针对不同模型或任务预设阈值，缺乏自适应调整机制。
- **攻击类型覆盖可能不全**：未明确说明是否覆盖所有类型的提示注入（如多轮注入、编码绕过等），可能需要额外验证。
- **实验细节不透明**：原文（仅摘要）未提供详细数据集、模型列表、对比方法名称、层选择实验等，削弱了可复现性。
- **仅针对特定目的代理**：方法依赖于预设的角色限制，对无特定政策限制的通用LLM可能不适用。
- **潜在偏差风险**：PV概念可能随模型版本、数据分布变化而漂移，需要定期更新离线集。

（完）
