---
title: "AgentGuard: An Attribute-Based Access Control Framework for Tool-Use LLM-Based Agent"
title_zh: AgentGuard：一种用于基于工具使用的LLM智能体的属性访问控制框架
authors: "Jiaqi Luo, Songyang Peng, Jiarun Dai, Zhile Chen, Zhuoxiang Shen, Geng Hong, Xudong Pan, Yuan Zhang, Min Yang"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28071v1"
tags: ["query:llm-security"]
score: 9.0
evidence: 基于属性访问控制的安全机制
tldr: LLM智能体在自动调用工具时面临严重安全风险，可能导致隐私泄露或系统被攻陷。本文提出AgentGuard，一种基于属性的访问控制框架，采用客户端-服务器架构，客户端仅需约10行代码修改即可集成，服务器端提供三种互补检查机制覆盖单工具和跨工具风险。该框架同时提供可视化界面用于策略制定和运行时审计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28071v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 828, \"height\": 454, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28071v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1855, \"height\": 576, \"label\": \"Table\"}]"
motivation: 现有LLM智能体工具调用存在隐私泄露、财务损失等安全漏洞，亟需有效的访问控制机制。
method: 提出AgentGuard框架，客户端轻量集成无需改变执行逻辑，服务器端设计三种互补检查机制（单工具与跨工具），并配有可视化安全策略界面。
result: 客户端仅需约10行代码修改即可集成，服务器端机制能有效覆盖单工具和跨工具安全风险。
conclusion: AgentGuard为工具使用LLM智能体提供了轻量、可扩展的基于属性访问控制，显著提升安全性。
---

## 摘要
基于LLM的智能体因其能够自主调用相关工具完成复杂任务而受到广泛关注。然而，最近的研究表明，这些智能体面临严重的安全风险，可能导致隐私泄露、经济损失甚至系统完全被攻破。在本文中，我们提出了AgentGuard，一种用于基于工具使用的LLM智能体的属性访问控制框架。AgentGuard采用客户端-服务器架构。在客户端，AgentGuard为用不同编程语言和架构实现的智能体提供轻量级集成。它仅需少量代码修改（例如约10行），无需改变底层智能体执行逻辑。在服务器端，AgentGuard提供三种互补的检查机制，以覆盖智能体执行中的单工具和跨工具安全风险。此外，它还提供可视化的前端界面用于安全策略规范和运行时审计。目前，AgentGuard可在https://github.com/WhitzardAgent/AgentGuard公开访问。

## Abstract
LLM-based agents have recently attracted significant attention due to their ability to autonomously invoke relevant tools to accomplish complex tasks. However, recent studies have shown that these agents face severe security risks, which may lead to privacy leakage, financial loss, or even full system compromise. In this paper, we present AgentGuard, an attribute-based access control framework for tool-use LLM-based agents. AgentGuard adopts a client-server architecture. On the client side, AgentGuard provides lightweight integration for agents implemented in different programming languages and architectures. It requires only minor code modifications (e.g., around 10 lines) without changing the underlying agent execution logic. On the server side, AgentGuard provides three complementary inspection mechanisms to cover both single-tool and cross-tool security risks in agent execution. In addition, it offers a visualized front-end interface for security policy specification and runtime auditing. Currently, AgentGuard is publicly accessible at https://github.com/WhitzardAgent/AgentGuard.

---

## 论文详细总结（自动生成）

好的，以下是对论文《AgentGuard: An Attribute-Based Access Control Framework for Tool-Use LLM-Based Agent》的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于LLM（大型语言模型）的智能体能够自主调用外部工具完成复杂任务，但面临严重的安全风险，如隐私泄露、财务损失甚至系统被完全攻陷。现有防御方法（模型级和系统级）存在三个主要不足：**风险覆盖不全**（多局限于单工具调用，忽略跨工具组合攻击）、**兼容性差**（难以无缝集成到现有不同语言和架构的智能体框架中）、**策略规范复杂**（往往需要大量手动编码或缺乏可视化支持）。
- **整体含义**：本文提出的AgentGuard旨在填补上述空白，为基于工具使用的LLM智能体提供轻量、可扩展、可视化的强制访问控制框架，从而在不改变原有执行逻辑的前提下，显著提升智能体的安全性。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用**客户端-服务器**架构，将安全控制逻辑从智能体的规划与执行中解耦。客户端负责拦截工具调用并转发上下文；服务器根据预定义的属性访问控制策略进行裁决，并返回允许/拒绝/审查的决策。
- **关键技术细节**：
  - **客户端侧**：
    - 提供多语言SDK，仅需约 **10行代码** 修改即可集成到LangChain、AutoGen、OpenAI Agents SDK等主流框架中。
    - 运行时监控每次工具调用，将主体信息（身份、会话、角色、信任级别）和工具属性（名称、参数、目标地址等）发送给服务器。
  - **服务器侧**：
    - **可视化前端界面**：允许用户通过参数化表单灵活配置安全策略，并支持运行时监控与审计。
    - **三种互补检查机制**：
      1. **基于规则的检测**（Rule-based）：对单工具调用进行静态规则匹配（如黑名单、参数范围限制）。
      2. **LLM辅助检测**（LLM-assisted）：利用辅助大模型对模糊或语义复杂的行为进行判断（如检测恶意意图）。
      3. **人工验证**（Manual verification）：对高风险或无法自动决策的调用触发人工审核。
    - **策略定义**：采用声明式策略语言（DSL），可针对主体身份、工具属性、参数、目标地址、会话历史等进行组合规则描述，覆盖**单工具**和**跨工具**安全风险。
- **架构流程**：工具调用 → 客户端拦截 → 发送上下文 → 服务器执行策略（规则/LLM/人工） → 返回决策 → 客户端执行（允许或终止调用）。

### 3. 实验设计

- **数据集/场景**：论文**未提供定量实验**（如数值结果、数据集、benchmark）。仅在**Table 1**中与现有工作（AgentDoG、ClawSentry、AgentBound、CaMeL、SECAgent、ACE、Agent Governance Toolkit、Progent、AgentSpec、IsolateGPT）进行了**特性对比**，涵盖风险覆盖（单工具/跨工具）、干预阶段（执行前/执行后）、决策机制（规则/LLM/人工）、兼容性（框架无关、语言无关、代理解耦、低侵入性）以及可用性（可视化规则设置与审计、动态规则更新）。
- **Benchmark**：未使用任何公开benchmark（如AgentDojo等）。
- **对比方法**：Table 1中列出了11种现有系统，但仅进行了定性比较，未进行性能或安全有效性的量化对比。

### 4. 资源与算力

- 论文中**未提及**任何关于GPU型号、数量、训练时长或推理资源的信息。该框架本身无需训练，属于运行时安全控制。

### 5. 实验数量与充分性

- **实验数量**：**无**。论文属于系统设计类短文（共3页），未开展任何消融实验、攻击成功率测试或性能开销评估。
- **充分性评价**：**不充分**。虽然通过Table 1展示了设计覆盖上的优势，但缺乏量化验证（如能否防御已知攻击、对智能体响应延迟的影响、误报率/漏报率等）。结论的可信度主要依赖于设计逻辑，而非实证数据。

### 6. 论文的主要结论与发现

- 提出了AgentGuard，一种轻量级的基于属性的访问控制框架，能够无缝集成到现有LLM智能体中。
- 通过客户端-服务器分离架构，实现了安全策略的集中管理、运行时监控与审计。
- 三种互补检查机制（规则、LLM、人工）能够同时覆盖单工具和跨工具安全风险。
- 可视化前端显著提升了策略配置和审计的易用性。
- 论文声称AgentGuard相比现有工作具有更广的风险覆盖、更灵活的决策机制、更好的兼容性和可用性（依据Table 1）。

### 7. 优点

- **轻量集成**：仅需约10行代码修改，无需改变智能体执行逻辑，支持多种主流框架和语言。
- **架构解耦**：客户端-服务器分离，允许多个智能体在统一策略视图下管理，且安全控制独立于智能体自身。
- **全面的风险覆盖**：同时支持单工具和跨工具安全检测，优于大部分仅关注单工具的系统。
- **多机制协同**：规则检测提供确定性，LLM辅助提供灵活性，人工验证提供最终兜底，适合不同风险等级。
- **高可用性**：提供可视化前端，用户无需编写代码即可配置策略并查看审计日志，降低了使用门槛。
- **开放源码**：项目已在GitHub上以MIT许可证发布，便于社区复现和扩展。

### 8. 不足与局限

- **缺乏实证验证**：论文未提供任何定量实验（如攻击防御成功率、性能开销、误报率），无法客观评估其实际效果。
- **风险覆盖不完整**：仅关注工具调用层面，未涉及LLM的输入/输出层面的安全（如提示注入检测），作者也将其列为未来工作。
- **未讨论多智能体场景**：仅支持单智能体，多智能体协作场景下的安全策略协调尚未支持。
- **策略表达能力**：声明式DSL虽简化了配置，但可能无法覆盖复杂或动态的组合攻击模式，需要结合LLM辅助。
- **人工验证延迟**：在高吞吐场景下，人工验证可能成为瓶颈，论文未讨论相关优化或降级策略。
- **兼容性限制**：虽然声称框架无关，但适配器目前仅支持有限的主流框架，其他框架可能需要额外开发。
- **资源开销**：客户端-服务器网络通信引入额外延迟，论文未评估其对智能体响应时间的影响。

（完）
