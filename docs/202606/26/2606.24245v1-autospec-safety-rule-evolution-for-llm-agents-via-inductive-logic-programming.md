---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: AutoSpec：通过归纳逻辑编程实现LLM智能体的安全规则演化
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v1"
tags: ["query:llm-security"]
score: 8.0
evidence: 自主智能体安全机制，自动安全规则演化
tldr: "LLM智能体在执行任务时存在安全风险，手工规则或神经网络方法难以平衡假阳性与假阴性，且缺乏可解释性。AutoSpec提出基于反例引导归纳合成（CEGIS）和归纳逻辑编程（ILP）的框架，从专家规则和用户标注中自动演进安全规则。通过ILP高效识别区分正反例的谓词，剪枝搜索空间，在代码执行和具身智能体域上F1达到0.98和0.93，假阳性降低94%，4-5次迭代收敛，规则可解释且泛化良好。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24245v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 629, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 661, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24245v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 559, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 256, \"label\": \"Table\"}]"
motivation: 现有LLM智能体安全规则存在高假阳性或高假阴性问题，且神经方法缺乏可解释性，需要自动生成平衡精度与召回率的可解释规则。
method: AutoSpec采用CEGIS循环评估规则、挖掘反例，并通过ILP学习区分正反例的谓词以生成候选规则编辑，迭代优化直至收敛。
result: "在291条执行轨迹上，规则F1提升至0.98和0.93，假阳性降低94%，4-5次迭代收敛，比启发式CEGIS方法F1高4.8倍。"
conclusion: AutoSpec能自动生成可解释、可审计且泛化性强的安全规则，有效平衡规则精度与召回率，提升LLM智能体安全性。
---

## 摘要
大型语言模型（LLM）智能体通过将语言模型与外部工具和环境集成，越来越多地实现复杂任务的自动化。然而，它们的自主性带来了显著的安全风险：智能体可能执行破坏性命令、泄露敏感数据或违反领域约束。现有的安全方法面临一个基本权衡：手工设计的规则可解释但脆弱，过于保守的规则会阻碍安全操作（高误报率），而宽松的规则会遗漏不安全行为（高漏报率）。神经分类器缺乏安全关键部署所需的可解释性。我们提出了AutoSpec，这是一个通过反例引导的归纳综合（CEGIS）并由归纳逻辑编程（ILP）引导，从用户安全/不安全标注中自动演化部署的专家设计安全规则的框架。从专家规则和一系列带标注的轨迹开始，AutoSpec迭代评估规则，挖掘误报和漏报反例，使用ILP学习哪些谓词可以区分它们，生成候选规则编辑，并验证候选规则以选择最佳修订。关键洞察在于ILP能高效识别在漏报中频繁出现但在误报中很少出现的谓词（或反之），从而大幅剪枝规则编辑的指数级搜索空间。该过程持续到收敛，产生平衡精确率和召回率的可解释规则。我们在涵盖代码执行和具身智能体领域的291条执行轨迹上评估了AutoSpec。AutoSpec在两个领域将规则的F1值分别提升至0.98和0.93，在保持高召回率的同时将误报率降低高达94%，并在4-5次迭代内收敛。ILP引导的方法相比启发式CEGIS实现了高达4.8倍的F1提升。学习到的规则可读、可审计，并能泛化到未见场景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.

---

## 论文详细总结（自动生成）

# 论文《AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：LLM 智能体在自动执行复杂任务（如代码执行、具身机器人控制）时存在显著安全风险（如执行破坏性命令、泄露敏感数据）。现有安全方法面临两难：手工规则可解释但静态且脆弱，易产生高假阳性或高假阴性；神经分类器虽自适应但缺乏可解释性，不适合安全关键部署。关键在于，专家设计的规则随模型更新、工具变化而快速过时，而智能体运行中积累的用户安全/不安全标注恰好能用于自动演化规则。
- **整体含义**：AutoSpec 将安全规则维护重新定义为从标注轨迹中自动演化已有专家规则的持续过程，而非手动一次性编写或从头合成。这是首个将反例引导归纳合成（CEGIS）与归纳逻辑编程（ILP）结合，在保持可解释性的同时提升安全规则精度与召回率的框架。

## 2. 方法论：核心思想与关键技术细节
### 核心思想
- 将规则演化问题形式化为 **CEGIS**：用户标注的误报（FP）和漏报（FN）作为反例，引导规则编辑的迭代搜索。
- 利用 **ILP** 高效学习区分反例的谓词（即频繁出现在漏报中但很少在误报中的谓词，或反之），从而大幅剪枝指数级规则编辑搜索空间。

### 关键技术细节
- **输入**：初始专家规则集 \(R_0\)，标注轨迹数据集 \(D_L = \{(\tau_i, y_i)\}\)（\(y_i \in \{ \text{safe}, \text{unsafe} \}\)），谓词库 \(P\)（每个域 16–24 个谓词，如 `destructive_os_inst`、`near_danger_zone`）。
- **规则结构**：每条规则 \(r = (g, \phi)\)，其中 \(g\) 是触发器（固定），\(\phi\) 是谓词合取；轨迹被判定为不安全当且仅当存在某规则在某个事件上触发。
- **工作流程（算法1）**：
  1. **反例挖掘**：评估当前规则集 \(R_i\) 在 \(D_L\) 上的分类，收集误报（FP）和漏报（FN）。
  2. **ILP 谓词学习**：将 FN 作为正例、FP 作为负例，用 ILASP 学习最小谓词组合 \(H\)（形如 \((p_{11} \land p_{12} \land \dots) \lor \dots\)），输出排名后的判别性谓词及建议编辑方向（如添加合取、添加例外、添加析取分支、放宽条件）。
  3. **候选生成与选择**：基于 ILP 建议，通过三种策略（单步编辑、混合修复链、失败驱动分支）生成候选规则集，并用得分 \(S(R') = (F1, Precision, -U, -|R'|)\) 按字典序选择最佳候选。
  4. **验证与迭代**：若候选优于当前规则，则更新；否则终止；同时引入软新颖性压力避免重复谓词导致停滞。

### 算法流程（文字描述）
- 循环最多 \(K\) 次迭代：
  - 若当前规则集得分达到阈值则返回。
  - 挖掘反例；若无反例则返回。
  - 通过 ILP 学习判别谓词（使用 ILASP，120 秒超时）。
  - 生成候选编辑（共 3 种策略）。
  - 选择得分最佳的候选；若优于当前则更新，否则返回。
- 返回最终规则集。

## 3. 实验设计
### 数据集与场景
- **代码执行域**：191 条轨迹（91 条 unsafe 来自 RedCode-Exec，覆盖 25 风险类别；100 条 safe 由 GPT-5.1-Codex 生成）。轨迹长度 1–9 事件（均值 1.4）。
- **具身智能体域**：100 条轨迹（52% unsafe）来自 SafeAgentBench（模拟 AI2-THOR 家庭机器人）。轨迹长度 1–15 事件（均值 5.1）。
- 所有轨迹由人工标注为 trace 级别安全标签。

### 对比方法
- **B1**：专家设计的初始规则（AgentSpec）。
- **B2**：LLM 分类器（基于 GPT 的 few-shot 分类器）。
- **B3**：无 ILP 指导的 CEGIS（使用基于覆盖率的启发式谓词排序）。
- **B4**：随机搜索（固定预算内随机采样候选）。

### 评估指标
- 精确率、召回率、F1（trace 级别）；收敛迭代次数；规则复杂度；候选效率。

### 实验协议
- **RQ1**（有效性）：在划分的种子集上演化，在保留测试集上评估。
- **RQ2**（效率）：在种子集上报告收敛轨迹（代码域从极弱规则启动，具身域从专家规则启动）。
- **RQ3**（泛化与可用性）：70%/30% 划分初始/新任务，评估泛化；10 名从业者进行可解释性用户研究。

## 4. 资源与算力
论文**未明确说明**使用的 GPU 型号、数量或训练时长。所有实验在 MacBook Pro (M2 Max, 32GB RAM) 上运行，ILP 求解器（ILASP）设 120 秒超时。AutoSpec 总体时间开销小于 21 秒（代码域 20.1s，具身域 11.8s），适合异步离线优化。

## 5. 实验数量与充分性
### 实验数量
- **RQ1**：在两个域上比较 5 种方法（AutoSpec + 4 个基线），报告精确率、召回率、F1、假正/假负数量。
- **RQ2**：显示收敛曲线（代码域 4 次迭代，具身域 5 次迭代）；对比 ILP 与启发式变体（迭代数、最终 F1、候选数、时间）。
- **RQ3**：泛化实验（初始 vs 新任务两组）；用户研究（10 名从业者 × 4 场景 × 2 方法 = 80 次比较，实际报告 120 次比较）。
- **消融实验**（表5）：在代码域上进行 4 组消融（去除 ILP、去除失败驱动分支、去除多步编辑、完整版本）。
- 整体共约 10 组主要实验（含消融和泛化）。

### 充分性与公平性
- **充分性**：覆盖两个异质域（代码+具身）；对比了专家规则、LLM 分类器、无 ILP 的 CEGIS、随机搜索；进行了消融和泛化实验；包含用户研究。
- **客观性**：指标清晰（F1、PR、迭代数等）；测试集保留下公正评估；泛化实验模拟真实部署。
- **公平性**：基线选择合理（与 AutoSpec 处于同一规则层）；但未与其他神经防护方法（如 Llama Guard 全监督版本）对比；用户研究样本较小（10人），但统计显著（Wilcoxon p<0.01）。

## 6. 主要结论与发现
1. **有效性**：AutoSpec 在两个域上 F1 分别达到 0.98（代码）和 0.93（具身），比专家规则提升 40%–89%，比无 ILP 的 CEGIS 提升 4.8 倍。
2. **搜索效率**：ILP 指导将最终 F1 从 0.203 提升至 0.980（代码域），从 0.758 提升至 0.933（具身域）；收敛仅需 4–5 次迭代。
3. **泛化能力**：规则从 70% 初始任务学习后，在 30% 新任务上 F1 仅下降 1.6%（代码）和 4.7%（具身），说明捕获了通用不安全模式。
4. **可解释性与信任**：用户评价中，AutoSpec 规则在可解释性（4.97 vs 3.45）、可操作性（4.83 vs 2.73）、信任度（4.80 vs 2.77）上均显著优于 LLM 分类器（p<0.01）。
5. **消融**：ILP 指导是最关键组件（去除后 F1 跌至 0.20）；多步编辑和失败驱动分支对效率和质量均有贡献。

## 7. 优点
- **概念创新**：首次将安全规则维护转化为 ILP 引导的 CEGIS 问题，利用运行中标注自动演化规则，保留可解释性。
- **技术亮点**：
  - ILP 高效剪枝指数级编辑空间，避免纯启发式搜索。
  - 设计多种编辑操作（合取、例外、析取、放宽）及混合修复链策略。
  - 引入软新颖性压力防止迭代停滞。
- **实验扎实**：覆盖两个不同域，对比多个基准，包含泛化测试和用户研究，消融验证各组件贡献。
- **实用性强**：规则最终形式与现有 AgentSpec 框架兼容，可直接部署；时间开销低（<21s），支持异步维护。

## 8. 不足与局限
- **实验覆盖有限**：仅测试代码执行和具身智能体两个域，未包含网页、数据库或网络代理等场景。
- **谓词库依赖**：规则表达能力受限于预定义谓词库；某些复杂时序或状态依赖行为需扩展谓词或加入时态逻辑。
- **全局最优性不保证**：算法为 NP-hard 问题，仅保证局部最优和单调改进；最坏情况下需专家介入补充谓词。
- **用户研究样本小**：仅 10 名从业者，虽然统计显著，但代表性可能受限。
- **标签依赖**：依赖人工标注（trace 级别），标注噪声可能影响演化质量；文中未讨论标签一致性或噪声鲁棒性。
- **未与全监督神经防护对比**：仅比较了 few-shot LLM 分类器，未对比 Llama Guard 等全监督模型在相同标注数据下的表现。

（完）
